import { AvoidwhitespacesDirective } from './avoidwhitespaces.directive';

describe('AvoidwhitespacesDirective', () => {
  it('should create an instance', () => {
    const directive = new AvoidwhitespacesDirective();
    expect(directive).toBeTruthy();
  });
});
